extends layout

block content
  h1= title
  p Welcome to #{title}
  p EPC Data: #{certificate} 
  p Smart Meter Data:
  if recommendations
    p Retrofitting Suggestions: #{recommendations}
  
  form(action="/logout?_method=DELETE" method="POST")
    input(type="submit" value="Log Out")
