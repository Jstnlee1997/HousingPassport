extends ../layout 
block append metas 
  meta(name="description" content="New user will key in postcode")

block content 
  h1= "Key in the postcode of your house!"
  form(action="/new-user/postcode" method="GET")
    p postcode: 
      input(type="text" name="postcode" placeholder= "e.g. SW6 7SR")
    input(type="submit")
  br
  br
  div(class="container")
    form(action="/logout?_method=DELETE" method="POST")
      button(type="submit" class="redirectbtn") Logout